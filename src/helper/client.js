'use strict';

/*import {SocketIOClient} from "../../typings/socket.io-client/socket.io-client.d";

export class Client {

    private socket: SocketIOClient;
    public connected: boolean = false;

    connect(nickname: string): void {
        this.socket = io('http://localhost:8081', { query: 'nickname=' + nickname });
        this.socket.on('connect', () => console.log('connected'));
    }
}*/
